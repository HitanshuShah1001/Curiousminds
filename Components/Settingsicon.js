import {Image} from 'react-native';
import { SETTINGS } from '../Sources/Imagesources';
export const Settingsicon = () => {
    return (
        <Image style={{height:20,width:20}} source={SETTINGS} />
    )
}